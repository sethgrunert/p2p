First run Server.java to start the server.
Next run Peer.java to start transferring data.
Peer.java accepts one parameter, an IP address in the form x.x.x.x
Slow mode for both the peer and the server side are hard coded Boolean values.
